export const metadata = {
  title: "Studio | Real Flutter",
  description: "Discover the best Flutter resources",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
